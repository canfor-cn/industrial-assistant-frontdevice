# ===================== Python 通用 =====================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/*
!lib/orbbec/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ===================== 虚拟环境 =====================
venv/
ENV/
env/

# ===================== IDE =====================
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# ===================== 系统文件 =====================
.DS_Store
Thumbs.db
desktop.ini

# ===================== 日志 =====================
*.log

# ===================== 训练模型（大文件）=====================
# NeMo 模型
*.nemo

# ONNX 模型（根目录的 OWW 训练模型）
xiaokang_*.onnx

# MediaPipe / 视觉模型
gesture_recognizer.task

# 训练 checkpoint
checkpoints/
training_checkpoints/
lightning_logs/
logs/

# ===================== 音频数据（大文件）=====================
# 所有音频文件
*.wav
*.mp3
*.flac
*.ogg

# 音频数据集目录
custom_dataset/
real_audio/
data/
results/

# MP3 目录中的音频文件（保留 .py 脚本）
MP3/*.mp3

# TTS 临时生成目录
_temp_tts_base/

# 数据集压缩包
custom_dataset.zip

# 训练 manifest（由脚本自动生成）
*_manifest.json
mixed_finetune_manifest.json
real_manifest.json

# ===================== 二进制库文件 =====================
*.dll
*.pyd

# 但保留内嵌的 Orbbec SDK（便携部署开箱即用）
!lib/orbbec/
!lib/orbbec/*.dll
!lib/orbbec/*.pyd

# ===================== 备份 / 临时文件 =====================
pip_backup.txt
conda_backup.yml
test_vision_cosyvoice_compat.py

# ===================== 特征文件 =====================
*.npy

# ===================== 测试录音和报告 =====================
test_recordings/
reports/

# ===================== 模型文件目录 =====================
models/

# ===================== 公开仓必须排除（隐私 / 大文件 / 凭据） =====================
# 运行时数据（含 visitor_sessions / interaction_logs / face/voice embedding 等隐私）
runtime/
!runtime/.gitkeep

# 用户上传 / 生成内容
uploads/
exhibit_wiki/
wiki-work/
wiki-test/

# 凭据 / 配置（绝不能进公开仓）
.env
.env.*
config.local.yaml
*.secret
*.pem
*.key

# 部署目录（含 EXE / Python / whl-cache）— 体积大，不入仓
dist/wakefusion-terminal/
deploy/

# Unity WebGL 构建产物（巨型二进制，npm postinstall 自动下载，见 README）
wakefusion_web/public/Build/Build.data
wakefusion_web/public/Build/Build.framework.js
wakefusion_web/public/Build/Build.loader.js
wakefusion_web/public/Build/Build.wasm
wakefusion_web/public/Build.bak/
wakefusion_web/public/Build.bak2/

# Tauri / cargo target
wakefusion_web/src-tauri/target/
wakefusion_web/src-tauri/Cargo.lock.bak

# Vite / npm
wakefusion_web/dist/
wakefusion_web/node_modules/
node_modules/
package-lock.json.bak

# 本地标准录音 / 测试音频
标准录音*.txt
test_recordings/

# 内部文档（非公开）
README-internal.md
